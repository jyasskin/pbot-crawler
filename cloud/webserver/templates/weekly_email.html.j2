<!DOCTYPE html>
<html>

<head>
  <title></title>
</head>

<body>
  <p>This is the result of a crawl of the PBOT website generated by <a
      href="https://github.com/jyasskin/pbot-crawler">https://github.com/jyasskin/pbot-crawler</a>.
    If you notice problems, please file issues there. If you want to stop getting
    these reports, <a href="[unsubscribe]">unsubscribe</a>. If a friend of yours
    wants to start getting reports, they can <a
      href="https://cdn.forms-content.sg-form.com/df6b15b2-4dd9-11ed-bc19-eef8bac46398">subscribe</a>.

  <p>Pages in the <a href="{{curr_crawl_link}}">{{curr_crawl_date}} crawl</a>, compared to the {{prev_crawl_date}}
    crawl.

  <p>All pages are under <code>https://www.portland.gov/</code>.

  <section>
    <h2>New pages</h2>

    <ul>
      {% for item in new %}
      <li><a href="{{item.page}}">{{item.page | without_origin}}</a> [<a
          href="{{item.page | web_archive(curr_crawl_date)}}">Archive</a>]
        {% endfor %}
    </ul>
  </section>

  <section>
    <h2>Removed pages</h2>
    <ul>
      {% for item in removed %}
      <li><a href="{{item.page}}">{{item.page | without_origin}}</a> [<a href="{{item.page | web_archive}}">Archive</a>]
        {% endfor %}
    </ul>
  </section>

  <section>
    <h2>Modified pages</h2>

    <p>See the <a href="{{modified_link}}">website</a> for detailed changes.</p>

    <ul>
      {% for item in modified %}
      <li><a href="{{item.page}}">{{item.page | without_origin}}</a> [<a href="{{item.page | web_archive}}">Archive</a>]
        {% endfor %}
    </ul>
  </section>
</body>

</html>
