<!DOCTYPE html>
<html lang="en">
<head>
  <title>PBOT Crawl Results</title>
  <style>
  </style>
</head>
<body>
  <h1>PBOT Crawl Results</h1>

  <p>Pages in the {{ crawl_date }} crawl, compared to the {{ prev_crawl_date }} crawl.

  <p>All pages are under <code>https://www.portland.gov/</code>.

  <section>
    <h2>New pages</h2>

    <ul>
      {% for item in new_pages().pages %}
        <li><a href="{{item.page}}">{{item.page | without_origin}}</a> [<a href="{{item.page | web_archive(crawl_date)}}">Archive</a>]
      {% endfor %}
    </ul>
    {% if new_pages().more_available %}
      <p><a href="{{more_new_pages}}">More new pages</a>
    {% endif %}
  </section>

  <section>
    <h2>Removed pages</h2>
    <ul>
      {% for item in removed_pages().pages %}
        <li><a href="{{item.page}}">{{item.page | without_origin}}</a> [<a href="{{item.page | web_archive(prev_crawl_date)}}">Archive</a>]
      {% endfor %}
    </ul>
    {% if removed_pages().more_available %}
      <p><a href="{{more_removed_pages}}">More removed pages</a>
    {% endif %}
  </section>

  <section>
    <h2>Modified pages</h2>
    <ul>
      {% for item in modified_pages().pages %}
        <li><a href="{{item.page}}">{{item.page | without_origin}}</a>
          [<a href="{{item.page | web_archive(prev_crawl_date)}}">Old</a>, <a href="{{item.page | web_archive(crawl_date)}}">New Archive</a>]
      {% endfor %}
    </ul>
    {% if modified_pages().more_available %}
      <p><a href="{{more_modified_pages}}">More modified pages</a>
    {% endif %}
  </section>
</body>
</html>
